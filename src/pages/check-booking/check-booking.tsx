import React from "react";
import styles from "./check-booking.module.scss";
import classNames from "classnames";

export default function CheckBooking() {
   return (
      <div className={classNames(styles.container, "fullFlexContainer")}>
         check booking
      </div>
   );
}
